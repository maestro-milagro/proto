syntax = "proto3";

package myservice;

service MyService {
    rpc GetData(RequestMessage) returns (ResponseMessage);
}

message RequestMessage {
    string query = 1;
}

message ResponseMessage {
    string result = 1;
}